#include <unistd.h>
#include <stdio.h>
void maff_alpha (void){
strdow='a';
while(strup<='z'){
if(strdow %2 == 1){    // operation of turning  odd alphabet a=97 and so on 
write(1,strdow,1):
}else{ char strupper = strdow -32; // for capital letter B=98 which is even number 
 write(1,&strdow,1);
}
strdow++;
}
int main(void){
maff_alpha();
return 0;   
}
