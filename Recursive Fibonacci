#include <stdio.h>

int fib(int n) {
    if (n <= 1)  // Base case
        return n;
    else
        return fib(n - 1) + fib(n - 2);  // Recursive calls
}

int main() {
    int n = 5;
    printf("Fib(%d) = %d", n, fib(n));
    return 0;
}
